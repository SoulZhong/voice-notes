# 云端 ASR(火山/阿里流式)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增「识别方式」维度——本地模型(现状)或云端流式 API(火山豆包 sauc / 阿里 DashScope fun-asr-realtime),欢迎页可选、设置页可改、断网缓存补识。

**Architecture:** 前处理链不变;云端模式下音频经 `CloudForwarder`(实现 Segmenter trait,零改动 segment_worker)转发给新的 `run_cloud_asr_worker`,由厂商服务端断句;interim → partial 预览,definite 句子 + 本地音频环形缓冲切片 → 抽取出的 `FinalSink`(回声去重/语言过滤/声纹/抑制记账全复用)。断连重连 + 缺口经本地 Silero 切段逐段批式补识。

**Tech Stack:** Rust(tokio-tungstenite WebSocket、flate2 gzip、ureq HTTP、crossbeam-channel)、Svelte 5。

**Spec:** `docs/superpowers/specs/2026-07-29-cloud-asr-design.md`(协议细节见其 §2.1,本计划引用为「spec §N」)。

## Global Constraints

- `asr_mode=local` 时全链路行为与现状逐字节一致;既有 lib 单测(816+)必须全绿。
- 所有新 settings 字段带 serde 默认,老 settings.json 零迁移。
- TDD:每个任务先写失败测试;协议编解码写成纯函数(字节/JSON 进出),单测不碰网络。
- 真实 API 测试一律 `#[ignore]`,凭证走环境变量 `VN_VOLC_APP_KEY`/`VN_VOLC_ACCESS_KEY`/`VN_DASHSCOPE_API_KEY`,缺失即 skip(测试内 early return + eprintln)。
- 注释风格遵循仓库现状:中文、讲"为什么"、不写"改了什么"。
- 常量集中:厂商端点/模型名/Resource-Id 全部 `const`,不散落字符串字面量。
- 每个任务独立 commit,消息格式 `feat(cloud-asr): …`,结尾带 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。

## File Structure(全景)

```
src-tauri/src/asr/cloud/mod.rs           类型/trait/工厂/MockCloudAsr(test)
src-tauri/src/asr/cloud/volcano/frames.rs 火山 v3 二进制帧编解码(纯函数)
src-tauri/src/asr/cloud/volcano/mod.rs    火山 WS adapter + flash 补识
src-tauri/src/asr/cloud/aliyun/protocol.rs 阿里 task 协议 JSON 构造/解析(纯函数)
src-tauri/src/asr/cloud/aliyun/mod.rs     阿里 WS adapter + qwen3-asr-flash 补识
src-tauri/src/session.rs                  FinalSink 抽取 + run_cloud_asr_worker
src-tauri/src/pipeline/cloud_forward.rs   CloudForwarder(实现 Segmenter)
src-tauri/src/settings.rs                 asr_mode/provider/凭证字段
src-tauri/src/models/mod.rs               status_for(云端就绪语义)
src-tauri/src/lib.rs                      模式分支/预载跳过/test_cloud_asr 命令
src/lib/models.ts + src/routes/settings/+page.svelte + src/lib/WelcomeOverlay.svelte
```

---

### Task 1: settings 云端字段

**Files:**
- Modify: `src-tauri/src/settings.rs`(字段区 ~L18-108,Default ~L145,tests 尾部)

**Interfaces:**
- Produces: `Settings.asr_mode: String`(`"local"`/`"cloud"`)、`Settings.cloud_asr_provider: String`(`"volcano"`/`"aliyun"`)、`Settings.volc_app_key/volc_access_key/dashscope_api_key: String`;常量 `pub const ASR_MODE_LOCAL/ASR_MODE_CLOUD/CLOUD_VOLCANO/CLOUD_ALIYUN: &str`。
- 后续所有任务判定云端凭证齐全用本任务的 `pub fn cloud_creds_ok(s: &Settings) -> bool`。

- [ ] **Step 1: 写失败测试**(settings.rs tests 模块尾部追加)

```rust
#[test]
fn cloud_asr_fields_default_local_and_roundtrip() {
    let tmp = tempfile::tempdir().unwrap();
    // 老配置缺字段 → local + volcano + 空凭证,老用户行为零变化。
    std::fs::write(tmp.path().join("settings.json"), r#"{"asr_model":"whisper"}"#).unwrap();
    let s = load(tmp.path());
    assert_eq!(s.asr_mode, ASR_MODE_LOCAL);
    assert_eq!(s.cloud_asr_provider, CLOUD_VOLCANO);
    assert!(s.volc_app_key.is_empty() && s.volc_access_key.is_empty() && s.dashscope_api_key.is_empty());
    assert!(!cloud_creds_ok(&s), "空凭证不算就绪");
    let s = Settings {
        asr_mode: ASR_MODE_CLOUD.into(),
        cloud_asr_provider: CLOUD_ALIYUN.into(),
        dashscope_api_key: "sk-x".into(),
        ..Default::default()
    };
    assert!(cloud_creds_ok(&s), "阿里只看 dashscope_api_key");
    save(tmp.path(), &s).unwrap();
    let got = load(tmp.path());
    assert_eq!(got.asr_mode, "cloud");
    assert_eq!(got.cloud_asr_provider, "aliyun");
    assert_eq!(got.dashscope_api_key, "sk-x");
    // 火山凭证要求两个都非空。
    let v = Settings { cloud_asr_provider: CLOUD_VOLCANO.into(), volc_app_key: "a".into(), ..Default::default() };
    assert!(!cloud_creds_ok(&v));
    let v = Settings { volc_app_key: "a".into(), volc_access_key: "t".into(), ..Default::default() };
    assert!(cloud_creds_ok(&v));
}
```

- [ ] **Step 2: 跑测试确认 RED**

Run: `cd src-tauri && cargo test --lib settings::tests::cloud_asr_fields 2>&1 | grep error`
Expected: `error[E0425]`/`E0609`(常量与字段不存在)

- [ ] **Step 3: 最小实现**

常量区(`ASR_QWEN3` 下方):

```rust
/// 识别方式:本地模型 / 云端 API(spec 2026-07-29-cloud-asr-design)。
pub const ASR_MODE_LOCAL: &str = "local";
pub const ASR_MODE_CLOUD: &str = "cloud";
/// 云端厂商标识。
pub const CLOUD_VOLCANO: &str = "volcano";
pub const CLOUD_ALIYUN: &str = "aliyun";
```

Settings 结构体(`asr_provider` 字段后追加):

```rust
    /// 识别方式:"local"(默认,现状) / "cloud"。录制中禁改(set_settings 保护)。
    #[serde(default = "default_asr_mode")]
    pub asr_mode: String,
    /// 云端厂商:"volcano" / "aliyun"。
    #[serde(default = "default_cloud_provider")]
    pub cloud_asr_provider: String,
    /// 火山凭证(APP ID / Access Token)。明文存储,同 refine_api_key 先例。
    #[serde(default)]
    pub volc_app_key: String,
    #[serde(default)]
    pub volc_access_key: String,
    /// 阿里 DashScope API Key。明文,同上。
    #[serde(default)]
    pub dashscope_api_key: String,
```

默认函数 + Default impl 补五个字段(`asr_mode: default_asr_mode()` 等):

```rust
fn default_asr_mode() -> String { ASR_MODE_LOCAL.into() }
fn default_cloud_provider() -> String { CLOUD_VOLCANO.into() }

/// 当前选中厂商的凭证是否齐全(云端模式录制就绪的必要条件)。
pub fn cloud_creds_ok(s: &Settings) -> bool {
    match s.cloud_asr_provider.as_str() {
        CLOUD_ALIYUN => !s.dashscope_api_key.trim().is_empty(),
        _ => !s.volc_app_key.trim().is_empty() && !s.volc_access_key.trim().is_empty(),
    }
}
```

- [ ] **Step 4: 跑测试确认 GREEN + 全量不回归**

Run: `cargo test --lib settings:: 2>&1 | grep "test result"`
Expected: 全部 ok,0 failed

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/settings.rs
git commit -m "feat(cloud-asr): settings 增加识别方式/厂商/凭证字段"
```

---

### Task 2: models 就绪语义感知 asr_mode

**Files:**
- Modify: `src-tauri/src/models/mod.rs`(`status()` ~L321 旁新增 `status_for`;tests)
- Modify: `src-tauri/src/lib.rs:3160` `models_status` 命令改调 `status_for`

**Interfaces:**
- Consumes: Task 1 的 `settings::{ASR_MODE_CLOUD, cloud_creds_ok}`。
- Produces: `pub fn status_for(asr_model: &str, cloud_mode: bool, creds_ok: bool) -> ModelsStatus`。云端模式:`required_for_recording` 仅 vad 为 true(spec §5:vad 保持恒必需,ASR 大模型全不必需);`recording_ready = vad present && creds_ok`。`status()` 保留为 `status_for(m, false, false)` 的等价旧入口(现有调用点不破)。

- [ ] **Step 1: 写失败测试**

```rust
#[test]
fn status_for_cloud_mode_needs_only_vad_plus_creds() {
    // 本地模式 = 现状(回归锚)。
    let local = status_for(crate::settings::ASR_SENSE_VOICE, false, false);
    assert_eq!(local.recording_ready, status(crate::settings::ASR_SENSE_VOICE).recording_ready);
    // 云端模式:ASR 工件全不必需,vad 仍必需。
    let cloud = status_for(crate::settings::ASR_SENSE_VOICE, true, true);
    for a in &cloud.artifacts {
        let want = a.id == "vad";
        assert_eq!(a.required_for_recording, want, "云端模式 {} 必需性", a.id);
    }
    // 凭证缺失 → 即使 vad 在也不就绪。
    let no_creds = status_for(crate::settings::ASR_SENSE_VOICE, true, false);
    assert!(!no_creds.recording_ready, "云端无凭证不可开录");
}
```

- [ ] **Step 2: RED** — Run: `cargo test --lib models::tests::status_for_cloud 2>&1 | grep error`,Expected: `status_for` 未定义。

- [ ] **Step 3: 实现**

```rust
/// 就绪判定的模式感知入口。cloud_mode=false 时与 status() 完全等价(本地现状);
/// cloud_mode=true:识别在云端,本地 ASR 大模型全不必需,vad 保留必需(spec §5:
/// 避免 required_now 分叉、回切本地时不缺件);录制就绪还要求凭证齐(creds_ok)。
pub fn status_for(asr_model: &str, cloud_mode: bool, creds_ok: bool) -> ModelsStatus {
    let mut s = status(asr_model);
    if cloud_mode {
        for a in &mut s.artifacts {
            a.required_for_recording = a.id == "vad";
        }
        let vad_ok = s.artifacts.iter().find(|a| a.id == "vad").map(|a| a.present).unwrap_or(false);
        s.recording_ready = vad_ok && creds_ok;
    }
    s
}
```

lib.rs `models_status` 命令改为:

```rust
#[tauri::command]
fn models_status(app: AppHandle) -> models::ModelsStatus {
    let s = app.path().app_data_dir().map(|d| settings::load(&d)).unwrap_or_default();
    models::status_for(
        &s.asr_model,
        s.asr_mode == settings::ASR_MODE_CLOUD,
        settings::cloud_creds_ok(&s),
    )
}
```

(`current_asr` 保留,托盘等其它调用点不动——它们只关心本地选型;托盘 recording_ready 的云端感知在 Task 10 统一处理。)

- [ ] **Step 4: GREEN + 全量** — Run: `cargo test --lib models:: 2>&1 | grep "test result"`,Expected: ok。
- [ ] **Step 5: Commit** — `git add -A src-tauri/src && git commit -m "feat(cloud-asr): 模型就绪语义感知云端模式"`

---

### Task 3: asr/cloud 类型层 + MockCloudAsr + 依赖

**Files:**
- Create: `src-tauri/src/asr/cloud/mod.rs`
- Modify: `src-tauri/src/asr/mod.rs:1`(`pub mod cloud;`)
- Modify: `src-tauri/Cargo.toml`(依赖)

**Interfaces:**
- Produces(后续任务全量依赖,签名精确):

```rust
pub struct CloudWord { pub text: String, pub start_ms: u64, pub end_ms: u64 }
pub struct DefiniteUtterance { pub text: String, pub start_ms: u64, pub end_ms: u64,
                               pub words: Vec<CloudWord>, pub lang: String }
pub enum CloudEvent { Interim { text: String }, Definite(DefiniteUtterance),
                      Closed { error: Option<String> } }
pub struct CloudStream {
    pub push: Box<dyn FnMut(&[f32]) -> anyhow::Result<()> + Send>,
    pub finish: Box<dyn FnOnce() -> anyhow::Result<()> + Send>,
    pub events: crossbeam_channel::Receiver<CloudEvent>,
}
pub trait CloudAsr: Send + Sync {
    fn open_stream(&self) -> anyhow::Result<CloudStream>;
    fn transcribe_batch(&self, samples: &[f32]) -> anyhow::Result<Vec<DefiniteUtterance>>;
}
pub fn f32_to_pcm_s16le(samples: &[f32]) -> Vec<u8>;   // 共两家 adapter 用
#[cfg(test)] pub struct MockCloudAsr { … }              // 见 Step 3
```

- [ ] **Step 1: 加依赖**

```bash
cd src-tauri
cargo add tokio-tungstenite --features rustls-tls-native-roots
cargo add flate2
cargo add uuid --features v4
```

- [ ] **Step 2: 写失败测试**(cloud/mod.rs 内嵌 tests)

```rust
#[test]
fn f32_to_pcm_s16le_clamps_and_encodes_little_endian() {
    let pcm = f32_to_pcm_s16le(&[0.0, 1.0, -1.0, 2.0]);
    assert_eq!(pcm.len(), 8);
    assert_eq!(&pcm[0..2], &[0x00, 0x00]);
    assert_eq!(&pcm[2..4], &(i16::MAX).to_le_bytes());       // 1.0 → 32767
    assert_eq!(&pcm[4..6], &(-32768i16).to_le_bytes());      // -1.0
    assert_eq!(&pcm[6..8], &(i16::MAX).to_le_bytes());       // 超界钳制
}

#[test]
fn mock_cloud_asr_replays_scripts_per_stream_and_batch() {
    let mock = MockCloudAsr::new(
        vec![vec![CloudEvent::Interim { text: "你".into() },
                  CloudEvent::Closed { error: Some("net".into()) }],
             vec![CloudEvent::Closed { error: None }]],
        vec![Ok(vec![DefiniteUtterance { text: "补".into(), start_ms: 0, end_ms: 500,
                                          words: vec![], lang: String::new() }])],
    );
    let s1 = mock.open_stream().unwrap();
    assert!(matches!(s1.events.recv().unwrap(), CloudEvent::Interim { .. }));
    assert!(matches!(s1.events.recv().unwrap(), CloudEvent::Closed { error: Some(_) }));
    let s2 = mock.open_stream().unwrap();
    assert!(matches!(s2.events.recv().unwrap(), CloudEvent::Closed { error: None }));
    assert!(mock.open_stream().is_err(), "脚本耗尽应报错(测试脚本写漏时快速暴露)");
    assert_eq!(mock.transcribe_batch(&[0.0; 160]).unwrap()[0].text, "补");
    assert_eq!(*mock.pushed_samples.lock().unwrap(), 0);
}
```

- [ ] **Step 3: 实现**(完整文件骨架)

```rust
//! 云端流式 ASR 适配层边界(spec §2)。厂商差异封装在 volcano/aliyun 子模块,
//! 会话层只见本文件的类型。协议编解码全部纯函数化,单测不碰网络。

pub mod volcano;
pub mod aliyun;   // Task 9 前先建空模块占位:`pub mod aliyun {}` 会编译失败,
                  // 故 Task 3 阶段本行与 volcano 行注释掉,Task 7/9 各自解开。

#[derive(Debug, Clone, PartialEq)]
pub struct CloudWord { pub text: String, pub start_ms: u64, pub end_ms: u64 }

#[derive(Debug, Clone, PartialEq)]
pub struct DefiniteUtterance {
    pub text: String,
    pub start_ms: u64,
    pub end_ms: u64,
    /// 逐词时间戳(有则喂 diarization 按词切分;空 → 段级降级,同本地 Qwen3)。
    pub words: Vec<CloudWord>,
    /// 厂商语种标签,通常为空 → 语言过滤走文本兜底。
    pub lang: String,
}

#[derive(Debug, Clone)]
pub enum CloudEvent {
    Interim { text: String },
    Definite(DefiniteUtterance),
    /// 连接终止。error=None 为正常关闭(finish 后);Some 触发重连状态机。
    Closed { error: Option<String> },
}

pub struct CloudStream {
    pub push: Box<dyn FnMut(&[f32]) -> anyhow::Result<()> + Send>,
    pub finish: Box<dyn FnOnce() -> anyhow::Result<()> + Send>,
    pub events: crossbeam_channel::Receiver<CloudEvent>,
}

pub trait CloudAsr: Send + Sync {
    fn open_stream(&self) -> anyhow::Result<CloudStream>;
    /// 批式补识(断网缺口):调用方先用本地 VAD 切段(≤15s),逐段调用;
    /// 返回时间戳相对段首,调用方叠加段偏移(阿里实现无时间戳,返回单条
    /// 覆盖 [0, 段长] 的 utterance,见 spec §2.1 偏差)。
    fn transcribe_batch(&self, samples: &[f32]) -> anyhow::Result<Vec<DefiniteUtterance>>;
}

/// f32 [-1,1] → PCM s16le 字节(两家线上格式)。超界钳制防 AGC 毛刺回绕。
pub fn f32_to_pcm_s16le(samples: &[f32]) -> Vec<u8> {
    let mut out = Vec::with_capacity(samples.len() * 2);
    for &x in samples {
        let v = (x.clamp(-1.0, 1.0) * 32767.0) as i16;
        out.extend_from_slice(&v.to_le_bytes());
    }
    out
}

#[cfg(test)]
pub struct MockCloudAsr {
    streams: std::sync::Mutex<std::collections::VecDeque<Vec<CloudEvent>>>,
    batches: std::sync::Mutex<std::collections::VecDeque<anyhow::Result<Vec<DefiniteUtterance>>>>,
    /// 推流样本计数(断言"断连期间没往死流推"用)。
    pub pushed_samples: std::sync::Arc<std::sync::Mutex<usize>>,
}

#[cfg(test)]
impl MockCloudAsr {
    pub fn new(stream_scripts: Vec<Vec<CloudEvent>>,
               batch_scripts: Vec<anyhow::Result<Vec<DefiniteUtterance>>>) -> Self {
        Self {
            streams: std::sync::Mutex::new(stream_scripts.into()),
            batches: std::sync::Mutex::new(batch_scripts.into()),
            pushed_samples: Default::default(),
        }
    }
}

#[cfg(test)]
impl CloudAsr for MockCloudAsr {
    fn open_stream(&self) -> anyhow::Result<CloudStream> {
        let script = self.streams.lock().unwrap().pop_front()
            .ok_or_else(|| anyhow::anyhow!("MockCloudAsr 流脚本耗尽"))?;
        let (tx, rx) = crossbeam_channel::unbounded();
        for ev in script { let _ = tx.send(ev); }
        // tx 随闭包存活:events 通道在 CloudStream drop 前不断开。
        let counter = self.pushed_samples.clone();
        Ok(CloudStream {
            push: Box::new(move |s: &[f32]| { *counter.lock().unwrap() += s.len(); let _ = &tx; Ok(()) }),
            finish: Box::new(|| Ok(())),
            events: rx,
        })
    }
    fn transcribe_batch(&self, _samples: &[f32]) -> anyhow::Result<Vec<DefiniteUtterance>> {
        self.batches.lock().unwrap().pop_front()
            .unwrap_or_else(|| Err(anyhow::anyhow!("MockCloudAsr 批式脚本耗尽")))
    }
}
```

`asr/mod.rs` 首行加 `pub mod cloud;`。volcano/aliyun 两行 mod 声明本任务先注释。

- [ ] **Step 4: GREEN** — `cargo test --lib asr::cloud 2>&1 | grep "test result"` → ok。
- [ ] **Step 5: Commit** — `git add -A src-tauri && git commit -m "feat(cloud-asr): 适配层类型/trait/Mock 与依赖"`

---

### Task 4: 从 run_asr_worker 抽取 FinalSink(纯重构)

**Files:**
- Modify: `src-tauri/src/session.rs`(worker 主体 ~L506-939 与结尾 Snapshot 导出段)

**Interfaces:**
- Produces:

```rust
pub(crate) struct FinalSink<'a> { /* 持有原 worker 的全部回声/声纹/表状态 */ }
impl<'a> FinalSink<'a> {
    fn new(embedder: &'a mut Option<Box<dyn SpeakerEmbedder>>, registry: &'a mut SpeakerRegistry,
           echo_hold: Duration, language_filter: bool,
           on_final: …, on_partial: …, on_diar: …) -> Self;
    /// 一条已识别的定稿段进入完整处理链(语言过滤/回声 hold/声纹/emit)。
    fn push(&mut self, source: Source, t: Transcript, start_ms: u64, end_ms: u64,
            samples: &[f32]);
    /// system 侧在途预览文本更新(预览级回声抑制依赖)。
    fn note_system_partial(&mut self, text: &str);
    /// 周期检查 pending mic 到期放行(原 recv_timeout 分支逻辑)。
    fn tick(&mut self);
    /// 排干 pending + 导出 Snapshot(原 worker 收尾段)。
    fn finish(self);
}
```

这是**机械搬移**:原 L522-533 的六组状态、`release_pending!` 宏、L566-585 的到期检查、L600-619 的语言过滤、回声即时/追溯比对(L668-842 区域)、worker 收尾的 Snapshot 导出,全部移入 FinalSink;`run_asr_worker` 缩为「取 job → catch_unwind recognize → sink.push / sink.note_system_partial / sink.tick」的壳。**不改任何行为**——session.rs 尾部 ~900 行 `asr_worker_tests` 是回归网,一行断言都不许改。

- [ ] **Step 1: 记录基线** — `cargo test --lib session:: 2>&1 | grep "test result"`,记下通过数 N。
- [ ] **Step 2: 实施搬移**(如上清单;`process_final` 维持自由函数,由 FinalSink::push 调用)。
- [ ] **Step 3: 验证零回归** — 同命令,通过数 == N,0 failed;再跑全量 `cargo test --lib`。
- [ ] **Step 4: Commit** — `git commit -am "refactor(session): 抽取 FinalSink,run_asr_worker 成壳(行为零变化)"`

---

### Task 5: CloudForwarder(实现 Segmenter)

**Files:**
- Create: `src-tauri/src/pipeline/cloud_forward.rs`
- Modify: `src-tauri/src/pipeline/mod.rs`(注册模块)

**Interfaces:**
- Consumes: `pipeline::segmenter::{Segment, Segmenter}`。
- Produces: `pub struct CloudForwarder { pub fn new(tx: crossbeam_channel::Sender<Vec<f32>>) -> Self }`,实现 `Segmenter`:`accept` 消毒后转发原始帧;`take_finished` 恒空;`current_partial` 恒 None;`flush` 无操作。segment_worker 因此零改动(spec §3)。

- [ ] **Step 1: 失败测试**

```rust
#[test]
fn forwards_sanitized_frames_and_never_yields_segments() {
    let (tx, rx) = crossbeam_channel::unbounded();
    let mut f = CloudForwarder::new(tx);
    f.accept(&[0.5, f32::NAN, f32::INFINITY]);
    assert_eq!(rx.recv().unwrap(), vec![0.5, 0.0, 0.0], "非有限值消毒(同 silero 入口)");
    assert!(f.take_finished().is_empty());
    assert!(f.current_partial().is_none());
    f.flush(); // 不 panic 即可
}
```

- [ ] **Step 2: RED** → **Step 3: 实现**

```rust
use super::segmenter::{Segment, Segmenter};

/// 云端模式的"分段器":不断句(厂商服务端 VAD 负责),只把前处理后的帧转发给
/// run_cloud_asr_worker。借 Segmenter 之形复用 run_segment_worker 全部逻辑
/// (AEC/暂停/电平),segment_worker 零改动。
pub struct CloudForwarder { tx: crossbeam_channel::Sender<Vec<f32>> }

impl CloudForwarder {
    pub fn new(tx: crossbeam_channel::Sender<Vec<f32>>) -> Self { Self { tx } }
}

impl Segmenter for CloudForwarder {
    fn accept(&mut self, samples: &[f32]) {
        // 与 silero.rs accept 同款消毒:NaN/Inf 会污染厂商解码(其 s16 转换回绕)。
        let clean: Vec<f32> = samples.iter().map(|&x| if x.is_finite() { x } else { 0.0 }).collect();
        let _ = self.tx.send(clean); // 接收端关闭 = 会话收尾,静默丢弃
    }
    fn take_finished(&mut self) -> Vec<Segment> { Vec::new() }
    fn current_partial(&mut self) -> Option<Vec<f32>> { None }
    fn flush(&mut self) {}
}
```

- [ ] **Step 4: GREEN + Commit** — `git commit -am "feat(cloud-asr): CloudForwarder 借 Segmenter 之形转发音频"`

---

### Task 6: run_cloud_asr_worker(核心,mock 驱动 TDD)

**Files:**
- Modify: `src-tauri/src/session.rs`(FinalSink 之后新增 worker + `SourceFeed`;tests 模块新增 `cloud_worker_tests`)

**Interfaces:**
- Consumes: Task 3 类型、Task 4 `FinalSink`。
- Produces:

```rust
/// 每源音频账本:推流样本计数(=流内时钟)+ 环形缓冲 + 断连缺口。
pub(crate) struct SourceFeed {
    fed: u64,            // 已推样本(16kHz),暂停不计 → 与本地时间轴同语义
    stream_base: u64,    // 当前流打开时的 fed(重连后厂商 ms 从 0 重计)
    ring: std::collections::VecDeque<f32>,
    ring_start: u64,     // ring[0] 的绝对样本号
    gap_from: Option<u64>,
}
pub(crate) const CLOUD_RING_CAP: usize = 5 * 60 * 16000; // 5 分钟(spec §4)

pub fn run_cloud_asr_worker(
    cloud: std::sync::Arc<dyn crate::asr::cloud::CloudAsr>,
    embedder: Option<Box<dyn SpeakerEmbedder>>, registry: SpeakerRegistry,
    audio_rxs: Vec<(Source, crossbeam_channel::Receiver<Vec<f32>>)>,
    echo_hold: Duration, language_filter: bool,
    backfill_segmenter: Box<dyn FnMut(&[f32]) -> Vec<(u64, Vec<f32>)> + Send>, // 缺口→(段偏移样本,段)列表
    on_final: impl FnMut(Source, String, u64, u64, Option<String>, Option<f32>),
    on_partial: impl FnMut(Source, String),
    on_diar: impl FnMut(DiarEvent),
    on_status: impl FnMut(CloudAsrStatus),   // Reconnecting{source}/Recovered/Backfilling/BackfillFailed
) -> Option<Box<dyn SpeakerEmbedder>>;
pub enum CloudAsrStatus { Reconnecting { source: Source }, Recovered { source: Source },
                          Backfilling { source: Source }, BackfillFailed { source: Source } }
```

行为规约(测试逐条锚定):

1. 每源开一条流;audio 帧到达 → `push` + 记账(fed/ring,ring 超 CAP 掐头)。
2. `Interim{text}` → `on_partial(source, text)`;system 源同时 `sink.note_system_partial(text)`。
3. `Definite{…}` → 绝对时间 = `stream_base*1000/16000 + vendor_ms`;按 [start,end] 切 ring 得声纹样本(超出 ring 范围给空);组 `Transcript { text, lang, tokens: words 文本, timestamps: words 相对秒 }` → `sink.push`。
4. `Closed{Some(e)}` → `gap_from = Some(fed)`,`on_status(Reconnecting)`;退避 `backoff_ms` 序列 1s,2s,4s…上限 30s(测试经 `#[cfg(test)]` 的 `CLOUD_BACKOFF_BASE_MS=1` 注入加速);重开成功 → `stream_base = fed`,缺口 [gap_from, fed) 音频从 ring 取出 → `backfill_segmenter` 切段 → 逐段 `transcribe_batch`,结果时间 = 缺口起点 + 段偏移 + 相对时间 → `sink.push`;`on_status(Recovered)`。补识任一段 Err → 整缺口一条 `[识别失败]` 占位段(start=缺口起点,end=缺口终点)+ `on_status(BackfillFailed)`。
5. 缺口超出 ring 覆盖:ring 之外的区间直接 `[识别失败]` 占位(spec §4)。
6. audio 通道全关闭(停录)→ 对活流 `finish`,3s 内排干剩余 Definite → `sink.finish()` 返回。
7. 断连期间到达的音频只记账不 push(流已死),不丢 ring。

- [ ] **Step 1: 写失败测试**(先 4 条核心;`ScriptedRecognizer` 风格,全部走 MockCloudAsr)

```rust
mod cloud_worker_tests {
    use super::*;
    use crate::asr::cloud::{CloudEvent, DefiniteUtterance, MockCloudAsr, CloudWord};

    fn utter(text: &str, s: u64, e: u64) -> DefiniteUtterance {
        DefiniteUtterance { text: text.into(), start_ms: s, end_ms: e, words: vec![], lang: String::new() }
    }
    /// 驱动骨架:单源 mic,收集 on_final/on_partial/on_status 到 Vec 断言。
    fn drive(mock: MockCloudAsr, frames: Vec<Vec<f32>>) -> (Vec<(Source,String,u64,u64)>, Vec<String>, Vec<String>) { /* 组装通道、喂帧、关通道、跑 worker、收集 */ }

    #[test]
    fn definite_maps_stream_ms_to_source_timeline_and_reaches_final() {
        let mock = MockCloudAsr::new(vec![vec![
            CloudEvent::Definite(utter("你好", 0, 500)),
            CloudEvent::Closed { error: None },
        ]], vec![]);
        let (finals, _, _) = drive(mock, vec![vec![0.0; 16000]]);
        assert_eq!(finals[0].1, "你好");
        assert_eq!((finals[0].2, finals[0].3), (0, 500));
    }

    #[test]
    fn interim_feeds_partial_slot() { /* Interim("你") → on_partial 收到 ("mic","你") */ }

    #[test]
    fn reconnect_records_gap_and_backfills_via_batch() {
        // 流1:立刻 Closed(err);流2:正常。批式脚本返回 utter("补",0,1000)。
        // backfill_segmenter 测试桩:整缺口一段,偏移 0。
        // 断言:on_status 依次 Reconnecting/Recovered/Backfilling? →(Backfilling 在 Recovered 前后均可,锚定实现后写死)
        // finals 含 ("补", 缺口起点ms, 缺口起点ms+1000)。
    }

    #[test]
    fn batch_failure_yields_placeholder_covering_gap() { /* transcribe_batch Err → "[识别失败]" 段 + BackfillFailed */ }
}
```

- [ ] **Step 2: RED**(`run_cloud_asr_worker` 未定义)。
- [ ] **Step 3: 实现 worker**(crossbeam `Select` 聚合 audio_rxs + 各流 events + 100ms tick;结构照上文规约,状态机注释写"为什么")。
- [ ] **Step 4: GREEN** — 4 条测试过;全量 `cargo test --lib` 不回归。
- [ ] **Step 5: 补边界测试并过**:缺口超 ring(占位)、停录排干(finish 后 Definite 仍落)、断连期间 pushed_samples 不增长(用 mock.pushed_samples 断言)。
- [ ] **Step 6: Commit** — `git commit -am "feat(cloud-asr): 云端识别 worker(时间映射/重连/补识)"`

---

### Task 7: 火山二进制帧编解码(纯函数)

**Files:**
- Create: `src-tauri/src/asr/cloud/volcano/frames.rs`(+ 目录 mod.rs 空壳 `pub mod frames;`,解开 cloud/mod.rs 的 `pub mod volcano;`)

**Interfaces:**
- Produces(Task 8 消费;协议依据 spec §2.1 火山节):

```rust
pub const WS_URL: &str = "wss://openspeech.bytedance.com/api/v3/sauc/bigmodel_async";
pub const RESOURCE_ID_STREAM: &str = "volc.bigasr.sauc.duration";
pub const FLASH_URL: &str = "https://openspeech.bytedance.com/api/v3/auc/bigmodel/recognize/flash";
pub const RESOURCE_ID_FLASH: &str = "volc.bigasr.auc_turbo";
pub fn full_request_frame(gzip_json: &[u8]) -> Vec<u8>;      // 0x11 0x10 0x11 0x00 + len + payload
pub fn audio_frame(pcm: &[u8], last: bool) -> Vec<u8>;       // 0x11 0x20/0x22 0x00 0x00 + len + pcm
pub fn full_request_json(app_hotwords: Option<&str>) -> serde_json::Value; // audio{pcm/16k/16/1}+request{bigmodel,itn,punc,show_utterances}
pub enum ServerFrame { Response { seq: i32, json: serde_json::Value }, Error { code: u32, msg: String } }
pub fn parse_server_frame(buf: &[u8]) -> anyhow::Result<ServerFrame>;   // 含 gzip payload 解压
pub fn utterances_from_response(json: &serde_json::Value) -> (Option<String>, Vec<crate::asr::cloud::DefiniteUtterance>);
// ↑ 返回 (整体 interim 文本, definite==true 的分句列表);words[] → CloudWord
```

- [ ] **Step 1: 失败测试**(golden 字节;帧头逐字节断言)

```rust
#[test]
fn audio_frame_header_layout_matches_v3_protocol() {
    let f = audio_frame(&[0xAB, 0xCD], false);
    assert_eq!(&f[..4], &[0x11, 0x20, 0x00, 0x00], "ver1|hdr1, audio|no-seq, raw|none");
    assert_eq!(&f[4..8], &2u32.to_be_bytes(), "大端长度");
    assert_eq!(&f[8..], &[0xAB, 0xCD]);
    let last = audio_frame(&[0x01], true);
    assert_eq!(last[1], 0x22, "末包 flags=0b0010");
}

#[test]
fn parse_server_response_with_gzip_and_definite_utterance() {
    let json = serde_json::json!({"result":{"text":"你好","utterances":[
        {"text":"你好","start_time":0,"end_time":700,"definite":true,
         "words":[{"text":"你","start_time":0,"end_time":300}]}]}});
    let payload = gzip(serde_json::to_vec(&json).unwrap());     // 测试助手
    let mut buf = vec![0x11, 0x90, 0x11, 0x00];                 // full response, JSON|gzip
    buf.extend_from_slice(&1i32.to_be_bytes());
    buf.extend_from_slice(&(payload.len() as u32).to_be_bytes());
    buf.extend_from_slice(&payload);
    let ServerFrame::Response { seq, json } = parse_server_frame(&buf).unwrap() else { panic!() };
    assert_eq!(seq, 1);
    let (interim, defs) = utterances_from_response(&json);
    assert_eq!(interim.as_deref(), Some("你好"));
    assert_eq!(defs[0].end_ms, 700);
    assert_eq!(defs[0].words[0].text, "你");
}

#[test]
fn parse_error_frame_yields_code_and_message() {
    let msg = "quota".as_bytes();
    let mut buf = vec![0x11, 0xF0, 0x00, 0x00];
    buf.extend_from_slice(&55000031u32.to_be_bytes());
    buf.extend_from_slice(&(msg.len() as u32).to_be_bytes());
    buf.extend_from_slice(msg);
    let ServerFrame::Error { code, msg } = parse_server_frame(&buf).unwrap() else { panic!() };
    assert_eq!(code, 55000031);
    assert_eq!(msg, "quota");
}
```

- [ ] **Step 2: RED** → **Step 3: 实现**(帧构造/解析照 spec §2.1 字节布局;`full_request_json` 含 `"result_type":"full"`、`"show_utterances":true`、hotwords 经 `corpus.context`)。
- [ ] **Step 4: GREEN + Commit** — `git commit -am "feat(cloud-asr): 火山 v3 帧编解码纯函数"`

---

### Task 8: 火山 adapter(WS + flash)

**Files:**
- Create: `src-tauri/src/asr/cloud/volcano/mod.rs`
- Create: `src-tauri/tests/cloud_volcano_it.rs`(`#[ignore]`)

**Interfaces:**
- Consumes: Task 7 全部;Task 3 类型。
- Produces: `pub struct VolcanoAsr { pub fn new(app_key: String, access_key: String) -> Self }` 实现 `CloudAsr`。

实现要点(完整写入代码,此处列结构):

- `open_stream`:起专用线程 + `tokio::runtime::Builder::new_current_thread().enable_all()`;`tokio_tungstenite::connect_async` 带鉴权 header(`X-Api-App-Key/X-Api-Access-Key/X-Api-Resource-Id/X-Api-Request-Id(uuid)/X-Api-Sequence: -1`);发 full_request_frame(gzip JSON);之后 select:内部 `tokio::sync::mpsc::Receiver<Vec<u8>>`(push 侧 `blocking_send` PCM,200ms 由调用节奏天然保证——segment_worker 帧率即节奏)→ 发 audio_frame;WS 消息 → `parse_server_frame` → interim/definite 映射 `CloudEvent` 经 crossbeam 发出;错误帧/断开 → `Closed{Some}`。`finish` → 发末包(last=true)后等服务端收尾再正常 `Closed{None}`。
- `transcribe_batch`:`ureq::post(FLASH_URL)` + 同族 header(Resource-Id 换 FLASH)+ JSON body(`audio.data` = base64(WAV 封装的 pcm);用 hound 内存写 WAV 头),响应 `utterances_from_response` 复用。
- 凭证/URL 均参数化,常量在 frames.rs。

- [ ] **Step 1: 失败单测**(不碰网络的部分):`VolcanoAsr::new` 后 `transcribe_batch(&[])` 应直接 `Err`(空音频本地拦截,不发请求)。RED → 实现 → GREEN。
- [ ] **Step 2: 实现完整 adapter**(如上结构;`cargo test --lib` 全绿,编译含 tokio 桥)。
- [ ] **Step 3: 真实 API ignored 测试**

```rust
// cargo test --test cloud_volcano_it -- --ignored --nocapture
#[test] #[ignore]
fn volcano_streams_chinese_fixture() {
    let (Ok(app), Ok(key)) = (std::env::var("VN_VOLC_APP_KEY"), std::env::var("VN_VOLC_ACCESS_KEY")) else {
        eprintln!("跳过:未设 VN_VOLC_APP_KEY/VN_VOLC_ACCESS_KEY"); return;
    };
    let asr = VolcanoAsr::new(app, key);
    let mut stream = asr.open_stream().unwrap();
    let samples = read_wav_mono_16k("tests/fixtures/sample_zh_16k.wav"); // 复用既有助手,拷贝进本文件
    for chunk in samples.chunks(3200) { (stream.push)(chunk).unwrap(); std::thread::sleep(std::time::Duration::from_millis(100)); }
    (stream.finish)().unwrap();
    let mut text = String::new();
    while let Ok(ev) = stream.events.recv_timeout(std::time::Duration::from_secs(10)) {
        match ev { CloudEvent::Definite(u) => text.push_str(&u.text),
                   CloudEvent::Closed { .. } => break, _ => {} }
    }
    assert!(text.chars().any(|c| ('\u{4e00}'..='\u{9fff}').contains(&c)), "应识别出中文: {text}");
}
#[test] #[ignore]
fn volcano_flash_batch_transcribes_fixture() { /* transcribe_batch(fixture) → 非空中文 */ }
```

- [ ] **Step 4: 有凭证则跑 ignored 验证;无凭证记录待办**。`cargo test` 全量回归。
- [ ] **Step 5: Commit** — `git commit -am "feat(cloud-asr): 火山流式/flash adapter"`

---

### Task 9: 阿里 protocol 纯函数 + adapter

**Files:**
- Create: `src-tauri/src/asr/cloud/aliyun/protocol.rs`、`src-tauri/src/asr/cloud/aliyun/mod.rs`
- Create: `src-tauri/tests/cloud_aliyun_it.rs`(`#[ignore]`)

**Interfaces:**
- Produces: `pub struct AliyunAsr { pub fn new(api_key: String) -> Self }` 实现 `CloudAsr`。protocol.rs:

```rust
pub const WS_URL: &str = "wss://dashscope.aliyuncs.com/api-ws/v1/inference/";
pub const REALTIME_MODEL: &str = "fun-asr-realtime";
pub const BATCH_URL: &str = "https://dashscope.aliyuncs.com/api/v1/services/aigc/multimodal-generation/generation";
pub const BATCH_MODEL: &str = "qwen3-asr-flash";
pub fn run_task_msg(task_id: &str) -> serde_json::Value;   // heartbeat:true, format pcm/16000
pub fn finish_task_msg(task_id: &str) -> serde_json::Value;
pub enum AliEvent { Started, Sentence { text: String, begin_ms: u64, end_ms: Option<u64>,
                                        sentence_end: bool, words: Vec<crate::asr::cloud::CloudWord>, heartbeat: bool },
                    Finished, Failed { code: String, msg: String } }
pub fn parse_event(text_frame: &str) -> anyhow::Result<AliEvent>;
pub fn batch_request_json(wav_base64: &str) -> serde_json::Value;   // data:audio/wav;base64,…
pub fn batch_text_from_response(json: &serde_json::Value) -> anyhow::Result<String>;
```

- [ ] **Step 1: protocol 失败测试**

```rust
#[test]
fn run_task_msg_carries_duplex_funasr_and_heartbeat() {
    let v = run_task_msg("tid");
    assert_eq!(v["header"]["action"], "run-task");
    assert_eq!(v["header"]["streaming"], "duplex");
    assert_eq!(v["payload"]["model"], "fun-asr-realtime");
    assert_eq!(v["payload"]["parameters"]["format"], "pcm");
    assert_eq!(v["payload"]["parameters"]["sample_rate"], 16000);
    assert_eq!(v["payload"]["parameters"]["heartbeat"], true, "防 60s 静默断连(spec §2.1)");
}

#[test]
fn parse_event_definite_sentence_with_words() {
    let raw = r#"{"header":{"task_id":"t","event":"result-generated","attributes":{}},
      "payload":{"output":{"sentence":{"begin_time":170,"end_time":1500,"text":"好的","heartbeat":false,
      "sentence_end":true,"words":[{"begin_time":170,"end_time":300,"text":"好","punctuation":"，"}]}},
      "usage":{"duration":3}}}"#;
    let AliEvent::Sentence { text, begin_ms, end_ms, sentence_end, words, heartbeat } = parse_event(raw).unwrap() else { panic!() };
    assert!(sentence_end && !heartbeat);
    assert_eq!((text.as_str(), begin_ms, end_ms), ("好的", 170, Some(1500)));
    assert_eq!(words[0].text, "好");
}

#[test]
fn parse_event_task_failed_carries_code() {
    let raw = r#"{"header":{"task_id":"t","event":"task-failed","error_code":"CLIENT_ERROR","error_message":"timeout","attributes":{}},"payload":{}}"#;
    assert!(matches!(parse_event(raw).unwrap(), AliEvent::Failed { .. }));
}
```

- [ ] **Step 2: RED → 实现 protocol.rs → GREEN**。
- [ ] **Step 3: 实现 adapter**:`open_stream` 同火山的线程+runtime 骨架;`Authorization: bearer <key>` header;文本帧发 run_task_msg,收到 `Started` 后开始把 push 队列的 PCM 以二进制帧发出;`Sentence{sentence_end:false,heartbeat:false}` → Interim;`sentence_end:true` → Definite(words 直接映射);`Failed` → `Closed{Some}`;finish → finish_task_msg → 等 `Finished` → `Closed{None}`。`transcribe_batch`:hound 内存 WAV → base64 → `ureq::post(BATCH_URL)`(Bearer)→ `batch_text_from_response` → 单条 `DefiniteUtterance{start_ms:0, end_ms: 段长ms, words:[], lang:""}`(无时间戳偏差,spec §2.1)。
- [ ] **Step 4: ignored 真实测试**(与火山同构:`VN_DASHSCOPE_API_KEY`;流式出中文 + batch 出中文)。
- [ ] **Step 5: 全量回归 + Commit** — `git commit -am "feat(cloud-asr): 阿里 fun-asr 流式/qwen3-flash 补识 adapter"`

---

### Task 10: lib.rs 装配(模式分支/预载/托盘/test_cloud_asr)

**Files:**
- Modify: `src-tauri/src/lib.rs`:开录流程(~L708 识别器取用处)、`start_session` 调用(~L1169)、`preload_models`(~L3106)、托盘 recording_ready(搜 `recording_ready` 调用点)、命令注册(~L4143)
- Modify: `src-tauri/src/session.rs` `start_session`(L1006):签名扩为接受 `enum AsrEngine { Local(Box<dyn Recognizer>), Cloud(std::sync::Arc<dyn CloudAsr>) }`,Cloud 分支为每源造 `CloudForwarder` 通道并 spawn `run_cloud_asr_worker`(backfill_segmenter 用 `SileroSegmenter` 包一层:accept 全部 → flush → take_finished 映射 (start 样本, samples))
- Produces: `fn make_cloud_asr(s: &Settings) -> anyhow::Result<std::sync::Arc<dyn CloudAsr>>`(按 provider 造 Volcano/Aliyun);`#[tauri::command] fn test_cloud_asr(app: AppHandle) -> Result<String, String>`(open_stream → 立即 finish → 等 Closed,成功返回厂商名文案;错误原样透出)。

- [ ] **Step 1: 失败测试**(session.rs:`start_session` 云端分支的装配用 mock 冒烟——已有 start_session 测试风格;lib.rs 纯装配逻辑以编译为主,`make_cloud_asr` 写单测:provider 字符串 → 期望类型的 Debug 名)。
- [ ] **Step 2: 实现**:
  - 开录:`asr_mode==cloud` 时不取 recognizer_cache(云端不需要),`make_cloud_asr` 失败(凭证缺失)→ 现有 fail 路径文案「请先在设置中配置云端凭证」;
  - `preload_models`:cloud 模式跳过识别器预载(声纹照常);
  - 托盘/前端 recording_ready 判定统一改走 Task 2 的 `status_for`;
  - `set_settings`:录制中修改 `asr_mode`/`cloud_asr_provider`/三个凭证字段 → 拒绝(沿 asr_changed 同款检查,消息「录制中不能切换识别方式」);
  - `on_status(CloudAsrStatus)` → `app.emit("cloud-asr-status", …)`(前端状态条,Task 11 消费)。
- [ ] **Step 3: 全量回归**(`cargo test` 0 failed;既有 816+ 必须全绿)。
- [ ] **Step 4: Commit** — `git commit -am "feat(cloud-asr): 会话装配/预载/托盘/测试连接命令"`

---

### Task 11: 设置页 UI

**Files:**
- Modify: `src/lib/models.ts`(Settings 类型加 5 字段;`testCloudAsr = () => invoke<string>("test_cloud_asr")`)
- Modify: `src/routes/settings/+page.svelte`(识别引擎 row ~L672 一带)

**Interfaces:** Consumes Task 1 字段名、Task 10 命令名(`test_cloud_asr`)。

- [ ] **Step 1: 实现**(核心标记,样式复用 seg/seg-item/row/link):

```svelte
<!-- 识别方式:本地 / 云端。录制中禁用(与引擎切换同款保护)。 -->
<div class="row">
  <div class="row-info">
    <span class="row-label">识别方式</span>
    <span class="row-desc">{asrMode === "cloud" ? "录音音频将实时上传至所选厂商" : "识别在本机完成,数据不出设备"}</span>
  </div>
  <div class="seg" class:disabled={recording.isLive}>
    <label class="seg-item"><input type="radio" name="asrmode" value="local" bind:group={asrMode}
      disabled={recording.isLive || !settings} onchange={() => changeAsrMode("local")} />本地模型</label>
    <label class="seg-item"><input type="radio" name="asrmode" value="cloud" bind:group={asrMode}
      disabled={recording.isLive || !settings} onchange={() => changeAsrMode("cloud")} />云端 API</label>
  </div>
</div>
{#if asrMode === "cloud"}
  <!-- 厂商 + 凭证 + 测试连接 -->
  <div class="row"> …火山/阿里 radio(bind:group={cloudProvider},onchange 走 saveSetting)… </div>
  {#if cloudProvider === "volcano"}
    <input class="ctl input" placeholder="APP ID" bind:value={volcAppKey} onblur={() => saveSetting((s) => (s.volc_app_key = volcAppKey))} />
    <input class="ctl input" type="password" placeholder="Access Token" bind:value={volcAccessKey} onblur={() => saveSetting((s) => (s.volc_access_key = volcAccessKey))} />
  {:else}
    <input class="ctl input" type="password" placeholder="DashScope API Key" bind:value={dashKey} onblur={() => saveSetting((s) => (s.dashscope_api_key = dashKey))} />
  {/if}
  <button class="btn-secondary" onclick={doTestCloud} disabled={testingCloud}>{testingCloud ? "测试中…" : "测试连接"}</button>
  {#if cloudTestResult}<span class="row-desc">{cloudTestResult}</span>{/if}
{:else}
  …现有四选一引擎 radio 原样(仅本地模式显示)…
{/if}
```

脚本侧:`asrMode/cloudProvider/volcAppKey/volcAccessKey/dashKey/testingCloud/cloudTestResult` 本地 $state,`syncLocalFromSettings` 补同步;`changeAsrMode` 仿 `changeAsr`(取新鲜值-改-写回-失败回弹);`doTestCloud` 调 `testCloudAsr()`,成功/失败写 `cloudTestResult`。录制页监听 `cloud-asr-status` 事件显示「云端识别中断,重连中/补识中」状态条(在 `src/routes/record/+page.svelte` 现有 status 条旁,同款样式)。

- [ ] **Step 2: `npm run check`** → 0 errors。
- [ ] **Step 3: 人工冒烟**(dev 运行:切换方式、录入凭证、测试连接按钮出结果)。
- [ ] **Step 4: Commit** — `git commit -am "feat(cloud-asr): 设置页识别方式/凭证/测试连接"`

---

### Task 12: 欢迎页分支 + README 措辞

**Files:**
- Modify: `src/lib/WelcomeOverlay.svelte`(phase 机 L31:`"choose" | "download" | "cloud-setup" | "learn" | "connect"`,初始 `choose`(未 onboarded 且模型未就绪时))
- Modify: `README.md`(隐私表述限定本地模式)

- [ ] **Step 1: 实现 choose 相位**:两张卡片——「本地识别 · 隐私优先」(→ 现有 download 流程)/「云端识别 · 更准更快」(→ cloud-setup:厂商选择+凭证输入+测试连接(复用 testCloudAsr),成功后 `saveSetting` 置 asr_mode=cloud 并只下载 vad+speaker(`downloadModels(["vad","speaker"])`)→ learn)。两卡片下注明「随时可在设置中更改」;云端卡明示「录音音频将上传至所选厂商」。
- [ ] **Step 2: README** — 「完全本地、隐私零外泄」改为「本地模式下完全本地…;可选云端识别(火山/阿里),音频实时上传至所选厂商,默认关闭」。
- [ ] **Step 3: `npm run check`** + 人工冒烟(清 onboarded 走一遍两分支)。
- [ ] **Step 4: Commit** — `git commit -am "feat(cloud-asr): 欢迎页本地/云端分支与隐私措辞"`

---

### Task 13: 收尾回归与文档

- [ ] **Step 1**: `cargo test`(全量,含 lib+集成)0 failed;`npm run check` 0 errors;有凭证则跑两家 ignored 真实测试。
- [ ] **Step 2**: `docs/asr-pipeline.md` 识别器一节补「云端模式」小节(CloudForwarder/FinalSink/重连补识,引用 spec);spec §9 勾掉已完成步骤。
- [ ] **Step 3**: 真机冒烟清单(人工):火山/阿里各录一段中英混说(partial 逐字、definite 落盘、说话人区分);录制中断网 30s(状态条+恢复补识);暂停 >60s 恢复(阿里 heartbeat 生效);纯云端新装引导(临时空模型目录)。
- [ ] **Step 4**: Commit — `git commit -am "docs(cloud-asr): 管线文档与冒烟清单"`

---

## Self-Review 记录

- **Spec 覆盖**:spec §1-§8 逐节对映 Task 1-13;§4 的 ring 5 分钟上限在 Task 6 常量;§2.1 阿里补识偏差在 Task 9 Step 3;§6 隐私措辞在 Task 11/12。
- **类型一致性**:`CloudAsr/CloudStream/CloudEvent/DefiniteUtterance/CloudWord` 定义于 Task 3,Task 6/7/8/9 消费签名逐一核对一致;`status_for` Task 2 定义、Task 10 消费;`cloud_creds_ok` Task 1 定义、Task 2/10 消费。
- **无占位符**:各任务代码块完整;Task 4 为搬移任务,以行号清单+回归网锚定(源代码在仓库内,非占位)。
