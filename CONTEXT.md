# 领域词汇

写代码、提 issue、做架构评审时用同一套词。这里只收**有歧义或容易说错**的词;实现细节看代码里的模块文档。

## 笔记与说话人

- **笔记(note)**:一场会议。目录 `notes/<id>/`,含 `meta.json`、`segments.jsonl`(原始稿段落)、`speakers.json`(说话人表)、`aing.json`(修订稿;`refined.json` 是旧名)。
- **说话人(speaker)**:一篇笔记内的一个声音,编号 `S1`、`S2`……只在本篇有意义。
- **说话人表**:`speakers.json`。每个说话人的本地名、人物关联、声纹建议、多人标记、占号。字段怎么联动只在 `store/speaker_table.rs` 定义。
- **本地名 / 库名**:本地名是用户在这篇里给说话人起的名字;库名是声纹库里那个人物的名字。关联后显示库名,本地名清空。
- **原始稿 / 修订稿**:原始稿是转写直出的段落(`segments.jsonl`);修订稿是 AI 整理(Aing)后的段落(`aing.json`),可编辑。

## 声纹库与认人

- **人物(person)**:声纹库里的一个人,编号 `P1`……跨笔记。库在 `voiceprints.json`。
- **指认 / 关联(link)**:把笔记里的说话人关联到一个人物。入口 `speaker_link::link_speaker_with`;手动关联、采纳识别建议、命名即入库都走它。
- **取消关联(unlink)**:只断开说话人与人物的绑定,表项与段落归属不动。**不等于删除说话人**。
- **改名即指认**:把已关联的说话人改成与库名不同的名字 = 「这不是那个人」:解除旧关联,再按命名即入库重新走。
- **命名即入库**:给无主说话人起名 = 确认身份:库里恰有一个同名人就关联它,没有就建人,重名则不猜。
- **确认才入库**(2026-08-27):声纹库只在用户确认动作之后被写。停录、识别推断都不写库。
- **确认样本**:用户确认过的那几段音频(试听过的、勾选「作为样本」的),切出来存为人物样本,合计至少 10 秒。样本是重建声纹的真源。
- **回灌(reinforce)**:把段落的声纹向量合进人物质心。整组回灌 = 本簇全部段;拆分产物只做确认段的单段回灌。
- **退样本(retire)**:关联改指别人或被取消时,把旧人从这一簇截的样本退掉并重建旧人声纹。
- **门内复核**:后台库写入在门(`FEEDBACK_GATE`)内再查一次「此刻仍是这个关联」才写。关联与取消关联各起后台任务,门只保证互斥、不保证顺序。
- **模型空间**:声纹向量只在同一嵌入模型内可比。嵌入器经 `open_speaker_embedder` 建出,带着自己的空间标签。

## 拆分与隔离

- **多人混杂**:一个说话人簇里其实不止一个人。打「多人」标后进入拆分流程。
- **隔离(quarantine)**:处置多人混杂期间,受影响人物的声纹暂停消费(不参与识别、不接受普通回灌)。
- **拆分 op**:一次拆分处置的持久化记录(`split_ops/`),按阶段推进,可从任一阶段断点续跑。规则在 `split_flow.rs`。
- **占号**:拆分提交前为新说话人预留的空表项(`reserved_by`),带 op 所有权。
- **拆分产物(split_born)**:由拆分创建的说话人。关联时永不做整组回灌。

## 占用

- **占用**:一篇笔记此刻被录制、Aing、重新转文字、补生成成品轨中的某件事占着。命令入口按「意图」判定放不放行,规则表在 `occupancy.rs`。
- **占槽 vs 持锁**:后台任务先占进程内的槽、稍后才取笔记目录锁。准入按占槽判定,整个任务期间一条规则。
